services:
  - type: web
    name: haleeco
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 5174
      - key: HOST
        value: 0.0.0.0
      - key: DEMO_MODE
        value: true
